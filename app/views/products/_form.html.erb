<%= render('layouts/errors', :object => @product) %>

<%= simple_form_for @product do |f| %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :country %>
  <%= f.submit :class => 'btn btn-success'%>
<% end %>
